import { requireAdmin } from "@/lib/authorization";
import { getRepository } from "@/lib/repository";
import { identifier,jsonBody,phone,text } from "@/lib/validation";
export async function GET(){try{await requireAdmin();return Response.json({schedule:await getRepository().getAdminSchedule()});}catch{return Response.json({message:"Unauthorized"},{status:401});}}
export async function POST(request:Request){try{await requireAdmin();const b=await jsonBody(request);const action=text(b.action,"Action",20),repository=getRepository();if(action==="toggle")await repository.toggleSlotBlocked(identifier(b.slotId,"Time slot"));else if(action==="cancel")await repository.cancelAppointment(identifier(b.appointmentId,"Appointment"));else if(action==="walk-in")await repository.createWalkInAppointment(identifier(b.slotId,"Time slot"),text(b.name,"Name",150),phone(b.phone));else if(action==="generate"){const interval=Number(b.intervalMinutes),buffer=Number(b.bufferEveryMinutes);if(!Number.isInteger(interval)||interval<5||interval>60)throw new Error("Interval must be between 5 and 60 minutes.");if(!Number.isInteger(buffer)||buffer<interval||buffer>180)throw new Error("Buffer interval is invalid.");await repository.generateScheduleForDay(text(b.date,"Date",10),text(b.startTime,"Start time",5),text(b.endTime,"End time",5),interval,buffer,typeof b.notes==="string"?b.notes.trim().slice(0,255):"");}else throw new Error("Unknown action.");return Response.json({schedule:await repository.getAdminSchedule()});}catch(error){const message=error instanceof Error?error.message:"Request failed.";return Response.json({message},{status:message==="Unauthorized"?401:400});}}
